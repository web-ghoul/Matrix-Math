export const metadata = {
  title: "Matrix Math - Branches",
  description: "App For Manage Students",
};

const BranchesLayout = ({ children }) => {
  return children;
};

export default BranchesLayout;
