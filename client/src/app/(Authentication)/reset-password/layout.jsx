export const metadata = {
  title: "Matrix Math - Reset Password",
  description: "App For Manage Students",
};

const resetPasswordLayout = ({ children }) => {
  return children;
};

export default resetPasswordLayout;
