export const metadata = {
  title: "Matrix Math - Forgot Password",
  description: "App For Manage Students",
};

const ForgotPasswordLayout = ({ children }) => {
  return children;
};

export default ForgotPasswordLayout;
