export const metadata = {
  title: "Matrix Math - Sign Up",
  description: "App For Manage Students",
};

const RegisterLayout = ({ children }) => {
  return children;
};

export default RegisterLayout;
