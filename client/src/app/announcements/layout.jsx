export const metadata = {
  title: "Matrix Math - Announcements",
  description: "App For Manage Students",
};

const AnnouncementsLayout = ({ children }) => {
  return children;
};

export default AnnouncementsLayout;
