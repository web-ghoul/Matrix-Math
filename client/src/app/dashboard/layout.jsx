import Main from "../Main";

export const metadata = {
  title: "Matrix Math - Dashboard",
  description: "App For Manage Students",
};

const DashboardLayout = ({ children }) => {
  return children;
};

export default DashboardLayout;
